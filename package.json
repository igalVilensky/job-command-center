{
  "name": "job-command-center",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "pnpm --filter @jobcc/web dev",
    "dev:api": "pnpm --filter @jobcc/api dev",
    "dev:ai": "pnpm --filter @jobcc/ai-service dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
